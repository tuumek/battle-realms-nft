{
  "name": "battle-realms-nft",
  "version": "1.0.0",
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^3.0.0",
    "@nomiclabs/hardhat-etherscan": "^3.0.0",
    "ethers": "^6.0.0",
    "hardhat": "^2.14.0",
    "@openzeppelin/contracts": "^4.9.0",
    "dotenv": "^16.0.0"
  },
  "scripts": {
    "compile": "hardhat compile",
    "deploy:baseSepolia": "hardhat run --network baseSepolia scripts/deploy.js",
    "deploy:base": "hardhat run --network base scripts/deploy.js"
  }
}
